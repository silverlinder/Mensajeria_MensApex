CREATE TABLE MENSAPEX_SRV_ARCS_MS_USER.ENTIDAD (
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
    NOMBRE          VARCHAR2(100 CHAR) NOT NULL,
    DESCRIPCION     VARCHAR2(255 CHAR),
    FECHA_CREACION  TIMESTAMP(6)
);
